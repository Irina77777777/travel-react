

//import slider from './Slider'

import Slider from "components/Slider/Slider"

type Props = {}
const Home = (props: Props) => {
    return (
       < Slider />
 //slider 
    ) 
}
export default Home